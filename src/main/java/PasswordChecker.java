public class PasswordChecker {

    private String url = "https://api.pwnedpasswords.com/range/";

    public boolean isPwnd(char[] password) {
        /*
          Use the PwndPasswords-API (see https://haveibeenpwned.com/API/v2#PwnedPasswords)
          to check if 'password' has previously been exposed in a data breach. Return 'true'
          if 'password' has been exposed, otherwise return 'false'.
          Clear all sensitive date before the method returns.
        */
    }
}
